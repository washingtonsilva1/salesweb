@model SalesWeb.Models.Seller

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>

<h4>Seller</h4>

<div class="row-no-gutters">
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(item => item.Name)</dt>
        <dd>@Html.DisplayFor(item => item.Name)</dd>
        <dt>@Html.DisplayNameFor(item => item.Email)</dt>
        <dd>@Html.DisplayFor(item => item.Email)</dd>
        <dt>@Html.DisplayNameFor(item => item.BirthDate)</dt>
        <dd>@Html.DisplayFor(item => item.BirthDate)</dd>
        <dt>@Html.DisplayNameFor(item => item.BaseSalary)</dt>
        <dd>@Html.DisplayFor(item => item.BaseSalary)</dd>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id"/>
        <input type="submit" class="btn btn-danger" value="Delete" />
        <a asp-action="Index">Back to List</a>
    </form>
</div>
